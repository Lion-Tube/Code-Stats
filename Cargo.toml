[package]
name = "code-stats"
version = "0.1.0"
edition = "2021"
authors = ["code-stats contributors"]
description = "أداة تحليل الكود المصدري للمشاريع البرمجية"

[dependencies]
clap = { version = "=4.4.18", features = ["derive"] }
colored = "=2.1.0"
walkdir = "2.4"
